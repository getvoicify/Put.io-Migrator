#!/usr/bin/env python3
"""
Put.io to NAS Migration Tool

Usage:
  putio-migrate [--config CONFIG_FILE] [--dry-run]
  
Options:
  --config CONFIG_FILE  Path to configuration file (default: config.toml)
  --dry-run            Scan and show what would be downloaded without downloading
"""

import sys
import os

# Add the project directory to Python path
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from putio_migrator.main import main

if __name__ == "__main__":
    main()